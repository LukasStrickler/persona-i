# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# ============================================================================
# DATABASE (Turso/LibSQL)
# ============================================================================
# Register at: https://turso.tech/
# 1. Sign up for a free account
# 2. Create a new database
# 3. Copy the database URL and token from the dashboard
DATABASE_URL="libsql://your-database-url"
DATABASE_TOKEN="your-database-token"

# ============================================================================
# AUTHENTICATION (Better Auth)
# ============================================================================
# Better Auth is a self-hosted authentication solution
# Documentation: https://www.better-auth.com/
# Generate a secure secret key (at least 32 characters):
# You can use: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-key-here-minimum-32-characters"
# Optional: Base URL for Better Auth (defaults to http://localhost:3000 in development)
BETTER_AUTH_URL="http://localhost:3000"
# Optional: Client-side Better Auth URL (defaults to http://localhost:3000)
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# ============================================================================
# EMAIL SERVICE (Resend)
# ============================================================================
# Register at: https://resend.com/
# 1. Sign up for a free account (100 emails/day free tier)
# 2. Verify your domain or use the test domain
# 3. Create an API key in the dashboard
# 4. Copy the API key here
RESEND_API_KEY="re_your-resend-api-key-here"
# The email address to send from (must be verified in Resend)
# For testing, you can use: onboarding@resend.dev
# For production, use your verified domain email (e.g., noreply@yourdomain.com)
RESEND_FROM="noreply@yourdomain.com"

# ============================================================================
# CONTACT FORM
# ============================================================================
# Email address where contact form submissions will be sent
# This is REQUIRED for the contact form to work
# Use a real email address that you can access
CONTACT_EMAIL="contact@yourdomain.com"

# ============================================================================
# BOT PROTECTION (hCaptcha)
# ============================================================================
# Register at: https://www.hcaptcha.com/
# 1. Sign up for a free account
# 2. Create a new site in the dashboard
# 3. Copy the Site Key (public) and Secret Key (private)
# 4. Add your domain to the allowed domains list
# Optional in development, REQUIRED in production
HCAPTCHA_SECRET_KEY="0x0000000000000000000000000000000000000000"
NEXT_PUBLIC_HCAPTCHA_SITE_KEY="10000000-ffff-ffff-ffff-000000000001"

# ============================================================================
# NODE ENVIRONMENT
# ============================================================================
# Usually set automatically by your deployment platform
# Options: "development", "test", "production"
# NODE_ENV="development"
